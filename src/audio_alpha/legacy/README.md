Legacy audio scripts from earlier pipeline work.

These files are kept for reproducibility and manual reruns. The package-style audio pipeline lives one level up in `src/audio_alpha/`; generated artifacts should go under `data/audio_alpha/`.
