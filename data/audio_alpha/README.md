Audio pipeline artifacts live here.

- `per_call_features/`: per-call Q&A feature JSONs generated from audio clips.
- `normalized/`: flattened and normalized acoustic feature tables plus speaker stats.
- `plots/`: visualizations generated from normalized audio features.
- `downloads/`, `qa_clips/`, and `transcript_cache/` are expected runtime folders for the legacy audio scripts when regenerated locally.
